#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define endl '\n'
#define all(x) (x).begin(), (x).end()
#define sz(x) int((x).size())

/*
    segment_tree con update en rango

    un buen tip es preguntarte, que pasa si aplico la operacion varias veces

    en este caso, la operacion aplicada varias veces seguidas sobre el mismo rango no cambiara nada

    realizar la operacion del rango uno por uno

    precalcular el fibonacci

    para encontrar el cercano hacer busqueda binaria
*/

void solve(int testcase){
    
}

int main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);

    int t = 1;
    cin >> t;
    for(int i = 0; i<t; i++){
        solve(i);
    }
}